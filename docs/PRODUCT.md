# プロダクトメモ

## アプリ：ラストピース（提案モック）

日本トレカセンター（JTCC）インターンのビジネスコンテスト「女性市場を開拓せよ」の提出物「02 動くモック」。

- **一文:** 即完売で買えなかった限定グッズを JTCC が買い取り・検品し、確率を公開したオリジナルガチャとして届ける。コレクションに足りない「最後の一点」を、友達といっしょに引き当てる。
- **誰に:** 推しが決まっていて、限定品を買えなかった経験がある 20〜30 代の女性（サンリオ・すみっコぐらし・ハイキュー!!・ちいかわのグッズ好き）。
- **体験の中心:** 一人で回さず、**リンクで友達を呼び、同じルームで回す**（最大4人）。当たりもおそろいも、その場でいっしょに見る。
- **ビジネスモデル:** JTCC 型（B2C）。JTCC が仕入れ・検品した在庫を、確率を公開したガチャで販売する。C2C ではない。

画面の設計は、チーム内の設計書「ラストピース アプリ UI 設計書（2026-09-25）」と、その試作（回す画面の試作5・画面設計 2/4 改訂）を移植したもの。

## 決まっていること

| 項目 | 決定 |
| --- | --- |
| 基盤 | React + Vite のスマホ向け Web アプリ（このリポジトリの基盤に合わせる） |
| 呼び方 | 「パック」ではなく「ガチャ」 |
| 作品（上のカード） | サンリオ／すみっコぐらし／ハイキュー!!／ちいかわ |
| グッズの種類（タブ） | すべて・ぬいぐるみ・アパレル・バッグ・アクセサリー・文房具・缶バッジ/アクスタ |
| 並び替え | おすすめ順／即完売品／残りわずか／新入荷 |
| 等級 | 画面に等級の文字（大当たり・中当たり等）は出さない。光り方で見分ける（目玉＝ローズゴールド、キラキラ＝パール）。詳細画面の「✦ 目玉」の見出しだけ使う。読み上げ用に隠し文字で区分を伝える |
| 確率 | 常に公開。**いまの残りから計算した値**を中身ごとに出す |
| 残り | **残り口数の数字（◯/◯）は出さない。** 数字なしの残りバーと言葉（たっぷり／半分くらい／残りわずか／売り切れ）を、ガチャ一覧のカードに出す。ガチャ詳細はマスター `267:8279` に合わせて残りバーを出さない（担当者の指示 2026-09-26「基本的にマスターを正として」、F11） |
| 回し方 | 1回ずつ。**回す前に確認**（必要なデモコイン・所持 → 確定後の残高）を出し、「◯使って1回引く」で確定したときに初めてコインを使って結果を決める。そのあと **3回転**。マスター `267:8348` どおり、右のハンドルをタップするか「1タップで1回転」ボタン（キーボード・読み上げ用）で1回転ずつ進める（以前の「指でなぞって回す」はやめた（担当者の指示 2026-09-26「基本的にマスターを正として」、F11））。回転・開封の途中は戻る・下のタブを出さない。連続で回す機能は作らない |
| 演出 | ひとりで回すときは、筐体はマスターの静止の絵（`267:10520`）で、回転しても筐体・背景・ピピは変わらない（担当者の指示 2026-09-26「基本的にマスターを正として」、F11）。友達と回す（デモ）は T10 の画面を作るまで以前の演出（背景の豪華化・ピピ・札）を残す。**確定演出は本当に目玉が出るときだけ**（ガセ演出はしない） |
| 開封 | カプセルをタップで開ける。マスター `267:8387`〜`8421` どおり、光り方にかかわらず **いつも3段階**（カプセルの見た目も同じ）（担当者の指示 2026-09-26「基本的にマスターを正として」、F11）。結果（`267:8438`）は品・使ったコインと残高・保存先と「街へ戻る」だけを出す（「もう1回まわす」「当てたものを見る」「¥◯◯相当」はやめた。相当額は当てたものの記録とみんなの結果で出す） |
| 友達と回す | ルームは最大4人。リンクを受け取った人だけが入れる（公開の募集はしない）。みんなの結果・おそろい・スタンプ・「いいな〜」 |
| コイン | **1コイン＝1円**。購入はデモ（押すと増えるだけ。決済は作らない）。はじめての人には体験用 3,000 コイン |
| 当てたもの | 倉庫ではなく記録。1つずつ「届けてもらう」か「コインに交換」 |
| コインに交換 | **参考価格の一律 20%**（端数切り捨て）。出金不可・取り消し不可。画面では「換金」と言わない |
| 案内役 | オリジナルキャラクター「ピピ」。実在キャラクターにはしゃべらせない |
| 見た目 | 配色「さくらミルク」（白〜パステルピンク）。演出時だけローズゴールドとパール。確定時はワインローズ。ロゴはリボン案 |
| 画像 | `public/assets/` のオリジナル生成素材だけを使う（ロゴ・作品・種類・UIアイコン・商品イラスト・バナー） |
| 声と音 | ピピの読み上げとチャイム・振動は、マイページで ON にしたときだけ鳴らす（最初は OFF） |
| 保存 | この端末の localStorage（キー `lastpiece_app_v1`）。壊れたデータは最初の状態に戻す。保存できない端末ではその旨を表示。F04 でお気に入りと「いいな〜」を追加（前の保存データは空として読む） |
| 棚（F04・F11） | **棚の枠＝1つのガチャの中身の種類**（マスター `267:8476` の9枠はサンリオ カプセルミックスの中身9種）（担当者の指示 2026-09-26「基本的にマスターを正として」、F11）。棚に出すのは、いちばん最近に回したガチャ（まだ回していなければサンリオ カプセルミックス）。枠の数と並びはそのガチャの中身と同じ（8〜12種）。持っている種類（手元にある・届け待ち）は絵と「持っている」、まだの種類は「＋」「未入手」と中身の名前。コインに交換した品は外れる。ほかのガチャの品は棚の外（当てたものの記録で見られる）。棚の中だけを縦にスクロールする |
| お気に入り（F04） | 持っている品に付け、いつでも外せる。枠は種類ごとに決まっているので、付けても位置は変わらない（F11） |
| フレンド（F04） | いっしょに回した友だち（デモのゆい・さき）だけが出る。ゆいの棚は固定のデモデータ、さきは「今は見られません」の状態見本。友だちの品は自分の持ち物に入らない |
| 共有（F04） | 「友だちからの見え方」は確認だけで、どこにも公開しない（「共有はまだ実行していません」）。見えるのはプレビューに出る棚の9枠だけ |
| 表示 | すべての入口に「提案モック・公式サービスではありません」と、商品・価格・在庫・確率が説明用の架空データである注記 |

## 共有ルーム（決定済み・2026-09-26、F07・F10）

提案の場で使う共有ルーム（[契約](SHARED_OPENING_CONTRACT.md)）の決定。画面は Figma の後に実装する（今は状態層と SQL だけ）。

| 項目 | 決定 |
| --- | --- |
| 人数 | ホストを含め最大 100 人（内部の上限）。画面には上限を出さず「N人が集まっています」だけを出す |
| 架空在庫 | 新しいルームは 300 個（目玉 30・ポーチ 90・缶バッジ 180。以前の 100 個と同じ比率で 3 倍） |
| 予約開始 | ホストが「1 / 3 / 5 / 10 分後に開始」を選ぶ。全員に開始時刻と秒読みを出す。開始前ならホストは変更・取り消しでき、「今すぐ開始」も使える。時刻になると、ホストが不在でもサーバーが開始する（その時点でオンラインの準備OKの人が対象）。予約はルームの期限（作成から 2 時間）の 1 分前まで。誰も準備していなかったなどで始まらなかったときは、ホストにその理由を出す |
| ピッチモード（ホストモード） | 対面のビジネスピッチで会場が反応できるよう、ホストがルームごとにオンにする。オンの間、各ラウンドで参加者 1 人に目玉を確定する（本物の在庫から出す）。全員の画面に「ピッチ用デモ：このラウンドは1人に目玉確定」などと明示する。目玉の在庫が 0 なら装わず、通常の抽選にして「確定なし」と分かるようにする |
| 確率の表示 | ピッチモード中は、公開している確率は確定枠には当てはまらない（確定枠以外の人には当てはまる）。画面はそのことを確率の近くに表示する |
| ホスト（F10） | ホストになれるのは担当者だけ。リンクは2種類で、参加者に配る招待リンクと、担当者だけが持つホスト用リンク（秘密のキー入り）。ホスト用リンクでだけルームを作れ、別の端末で開けばホストに戻れる（ほかの人の席・コイン・結果はそのまま）。参加者がホストを代わることはない（旧仕様の「ホスト交代」は廃止）。ホストがいなくても予約した開始は時刻に始まる。無効なリンクは「ホスト用リンクが無効」と案内し、ルームは作らない |
| 名前（F10） | 1〜12文字。ルームの中で重ならない（全角・半角、大文字・小文字、空白の違いは同じ名前とみなす）。名前を選ばずに入ると「ゲスト さくら12」のような名前が付き、入る前に見せる。使われている名前には「「もも2」で入る」のように空いている候補を出す。ロビーで名前を変えられる |

「確定演出は本当に目玉が出るときだけ」は変わらない。確定枠の目玉は実際に出る。

## 守ること（法務・ブランド）と、効かせ方

`scripts/governance.test.mjs`（`pnpm test` で実行・CI で落ちる）と、`src/domain/domain.test.ts` で確認する。

- 禁止語（換金・必ず当たる・還元率100%・大当たり・中当たり）を画面の文言に使わない。
- 「提案モック・公式サービスではありません」を各画面に出す。
- ピピのセリフ（`src/copy/pipi.ts`）に実在キャラクターの名前を入れない。
- 公開する画像はオリジナル素材の置き場所だけ（許可リスト）。サンリオ等の公式画像・ロゴ・キャラクター絵は使わない。キャラクター名は取り扱い商品の事実としてテキストだけ。
- 交換率は一律 20%。交換シートに「出金できません」「取り消せません」を出す。
- ガチャ詳細に残り口数の数字を出さない。確率の合計は 100%、残り 0 は 0%。
- 確定演出は目玉のときだけ（`shouldShowKakutei`）。
- 発売から 90 日以内の商品はカタログに入れない（`releasedAt` をテスト）。
- 友達へのコイン送付、賭け・勝ち負けの表示、「売る」機能は作らない。

## 画面（ハッシュで表す。固定版 URL でも再読み込みできる）

| URL | 画面 |
| --- | --- |
| `#/` | 街（ホーム）。URL にハッシュがない状態で開いたときだけ導入（約3秒・スキップ可、動きを減らす設定では1枚で自動では進まない）を出し、終わると `#/` に置き換える。街の絵がまだ準備できていなければ読み込み中を出す（F09） |
| `#/gacha` | ガチャのお店（ガチャ一覧） |
| `#/welcome` | はじめて（説明・ニックネーム・ホーム画面への追加の案内） |
| `#/gacha/<id>` | ガチャ詳細（マスター `267:8279`：目玉の候補といまの確率・1回のデモコインと引いた後の残高・「中身N種と確率を見る」「1回引く準備へ」。ルームの入口「友達と回す」も残す） |
| `#/gacha/<id>/odds` | 中身と確率（全種の確率を中身の順に表で出す。値はいまの残りから計算し、表示だけ小数第1位で合計100.0%に端数調整。F03・F11） |
| `#/gacha/<id>/room` | ルームを作る（招待リンク・待ち合わせ） |
| `#/gacha/<id>/spin` | ひとりで回す（回す前の確認 → 3回転 → 3段階の開封 → 今回の結果。売り切れ・コイン不足のときは理由と「ガチャ一覧に戻る」だけを出し、回さない） |
| `#/room/<code>` | 招待リンクの行き先（招待状 → ニックネーム → 待ち合わせ） |
| `#/room/<code>/spin` | みんなで回す → みんなの結果 |
| `#/together` | フレンド（いっしょに回した友だち（デモ）の棚への入口）。ルームはガチャ詳細の「友達と回す」から作る |
| `#/friend/<id>` | 友だち（デモ）の棚。見られない友だちは理由と戻り先を出す |
| `#/friend/<id>/<品のid>` | 友だちの1点と「いいな〜」（デモ・同じ品に1回だけ） |
| `#/shelf` | わたしの棚（最後に回したガチャの中身の種類の枠） |
| `#/shelf/<品のid>` | わたしの1点（お気に入りを付ける・外す） |
| `#/shelf/share` | 友だちからの見え方（共有前のプレビュー。公開はしない） |
| `#/collection` | 当てたもの（上はマスター `267:8623` の棚と同じ枠の一覧で、持っている品を先に。下に当てた記録：合計・絞り込み・コインに交換・届けてもらう。棚の「一覧」から来る） |
| `#/me` | マイページ（コイン・ニックネーム・声と音・お知らせ・きまり・デモ操作） |

下のタブはデザインマスターに合わせて **街・ガチャ・コレクション・フレンド** の4つ（F02）。コレクション → `#/shelf`（わたしの棚）、フレンド → `#/together`（フレンド）へつなぐ（F04）。街の地図の「コレクション」も棚へ行く。マイページはタブにせず、上部のコイン残高とアイコンから開く。

街の地図はマスターの部品（`267:10063`）のオリジナルのベクター画をコードの SVG で描き直したもの。マスター中央の内部キャラクター参照は案内役ピピに、注目のピースの商品写真は `public/assets/goods/` のオリジナルの商品イラストに置き換えた（内部素材は使わない）。地図の「当てたもの N」は、この端末の記録の数（マスターの「棚 2/9」はデモ例のため出さない）。

## 読み込み中（T07 Loading `267:8244`、F09）

- 街を出す前に待つのは、上部のコイン・マイページのアイコンと注目のピースの絵、使っている文字（フォント）だけ。街の地図はアプリに組み込んだ SVG なので待たない。読み込みはアプリを開いた直後に、どの画面から開いても始める。
- 待ちが 0.3 秒（`LOADING_DELAY`）続いたときだけ読み込み中を出す。それより早く準備できたら出さない（ちらつき防止）。わざと待たせる最低表示時間はない。導入のあとは、猶予のあいだ導入を出したままにする。
- 読み込み中は動きのない1枚。見出し「街の準備をしています」と文で状態を伝え、進み具合の数字は出さない（本当の割合が分からないため）。準備できたら自動で街へ進む。「スキップ」「街を見る」で先に街へ行ける（絵はあとから出る）。
- 読み込めなかったら「街の準備ができませんでした」と伝え、「もう一度読み込む」でやり直せる。先に街へも行ける。
- **Figma 修正待ち:** マスターの2文目「通信が必要な操作は、準備ができたらお知らせします。」は、デモに通信が必要な操作もお知らせの仕組みもないため、実際の動きどおり「準備ができたら、そのまま街へ進みます。」にした。失敗とやり直しの状態はマスターにないため追加した。担当者の確認後にマスターを先に直す。

## デモであること（実在のデータや動作を装わない）

- 友達（ゆい・さき）の入室・回転・結果は、この端末の中で再現している。画面に「デモ」と明記する。別のスマホとはまだつながらない。
- フレンドの棚（ゆい）・「今は見られません」（さき）・「いいな〜」はデモ。「いいな〜」はこの端末に記録するだけで、相手には届かない。棚の共有（公開）はしない。
- カタログ（4作品 × 3本 = 12本）・参考価格・在庫はすべて説明用の架空データ。
- コインの購入・発送はしない。「届けてもらう」は状態を「届け待ち」にするだけ。
- マイページの「次の1回を目玉確定にする」は審査用のデモ操作（1回で解除）。

## 設計書との違い（このリポジトリで判断したこと）

- 設計書は Next.js（`lastpiece-app/`）を想定していたが、このリポジトリの基盤（React + Vite・GitHub Pages）に合わせた。
- 画面の URL はハッシュ（`#/...`）にした。静的ホスティングの固定版 URL でも再読み込みできるようにするため。
- ホームのカードの「♡ ◯ルームで回してる」は出していない。実際のルーム数がないため（数字を装わない）。
- カタログは 1本あたり 8〜12種（設計書は 12〜24種）。

## チームの基盤ルール（main で決まっていること）

- React + Vite を基本とし、スマートフォンで利用する Web アプリを想定する。AI を活用して継続的に開発する。
- 本番の GitHub Pages は明示指示時のみ公開し、各バージョンを固定 URL と Release で残す。確認用サイトは [自動プレビュー](PREVIEW.md) として別途配布する。
- PR ベースで開発し、機能テストと UI/UX 基準の成功を自動承認・マージの条件にする。
- Apple HIG の Web に適用できる原則と、キャラクター／女性向けゲームの設計事例を UI/UX 基準に使う。
- デザインは指定 Figma ファイルで管理する。ラストピースの画面は、担当者の指示（2026-09-26）によりピンク基調の「さくらミルク」を正とし、[デザインマスター](ADOPTED_DESIGN.md)にまとめた。JTCC App 初版の緑系（`docs/DESIGN.md`）は雛形の記録で、ラストピースの主操作には使わない。

## 未決事項

- **配色（決定済み・2026-09-26）:** ピンク基調の「さくらミルク」を正とし、緑アクセントの版は不採用。主操作は `#be6482` のまま、ボタンの文字を 19px 太字（WCAG の大きい文字、白と 3.94:1 で 3:1 以上）にする。19px 未満のピンクの文字はワイン `#a94a68`（白と 5.43:1）。淡いミント `#E3F1E7`（タブの選択背景、コイン札、T10 の舞台背景）は残す。本文・補助文はマスターの `#172324`・`#52615a`（担当者の判断 2026-09-26、マスターを先に修正、アプリは F06 で反映。[DESIGN.md](DESIGN.md)）。
- **マスターの Web 実装（一部）:** T07 の街ホーム・導入・動きを減らす設定と、共通の4タブは F02 で実装した。T07 の読み込み中（`267:8244`）は F09 で実装した（街の絵が本当にまだ準備できていないときだけ出す。下の「読み込み中」）。T09 の棚・フレンド12状態は F04 で実装した（内部の商品写真はオリジナルの商品イラストに置き換えた）。色は担当者の判断（2026-09-26）で決まり、マスターを先に直した（主操作 `#be6482`＋19px 太字、小さい文字はワイン `#a94a68`、ミント `#E3F1E7` はそのまま。[変更履歴](ADOPTED_DESIGN.md)）。アプリへの反映は F06 で行った（`src/index.css` のトークンと共通のボタン・タブ）。T08 のガチャの流れ（一覧・詳細・確率・回す前の確認・3回転・3段階の開封・結果・売り切れ・コイン不足）は F03 で実装し、F11 でマスターとの違いをアプリ側で解消した（担当者の指示 2026-09-26「基本的にマスターを正として」。残す例外は [ADOPTED_DESIGN.md](ADOPTED_DESIGN.md)）。T10（ルーム）は未実装。ルームの上限は 100 人（ホスト込み）にし、画面には出さない（サーバー・状態層は F07 で反映済み）。
- **棚の枠と友だちの共有範囲:** 棚の枠は1つのガチャの中身の種類（担当者の決定 2026-09-26）。空き枠はマスターどおり中身の名前を出す。棚を見せる相手の選び方・招待・本当の共有は未決（T10 以降）。
- **T08・T09 のマスターとの違い（F11 で整理）:** 担当者の指示（2026-09-26「基本的にマスターを正として」）で、F03・F04 の「Figma 修正待ち」はアプリ側をマスターに合わせた。残すのは守ること（内部写真を使わない・デモ表記・確率を正直に出す・ガセの確定演出をしない）による例外と、既存の決定を守るために残した状態（ルームの入口、交換・届けてもらう、ガチャごとに違う中身の数など）だけで、[ADOPTED_DESIGN.md](ADOPTED_DESIGN.md) に理由とともに列挙。配色（主操作・小さいピンク文字・タブのミント）は F06 で共通トークンを切り替えて Web をマスターに合わせた。

- ルームの本当の同期（2〜4台をつなぐ）。設計書では Supabase Realtime（broadcast＋presence）を想定。そのときの在庫の扱い。
- 招待の「QRを見せる」、みんなの結果を「画像で保存・シェア」。
- 発送（届け先の入力）、決済・前払式支払手段の届出への対応、特商法・資金決済法の表示の中身。
- 抽選をサーバー側に移すか（本番化の段階）。
- 権利者（サンリオ等）との関係、取り扱い商品名の表示ルールの最終確認。
- 本番公開（GitHub Pages）のタイミング。本番は明示指示まで更新しない。確認用プレビューは CI 成功後に自動配布する。
- アクセス解析の要否。

## 次の機能を決めるときのテンプレート

### 課題

誰が、どの場面で、何に困っているか。

### 利用者の操作

開始から完了までの操作を短く書く。

### 受け入れ条件

- 利用者が確認できる結果を列挙する。
- エラー時や空状態の表示も定義する。

### 決定事項と保留事項

決めた理由と、後で判断する項目を記録する。
