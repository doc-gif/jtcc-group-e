# プロダクトメモ

## アプリ：ラストピース（開発中のサービス）

日本トレカセンター（JTCC）インターンのビジネスコンテスト「女性市場を開拓せよ」の提出物「02 動くモック」。

- **一文:** 即完売で買えなかった限定グッズを JTCC が買い取り・検品し、確率を公開したオリジナルガチャとして届ける。コレクションに足りない「最後の一点」を、友達といっしょに引き当てる。
- **誰に:** 推しが決まっていて、限定品を買えなかった経験がある 20〜30 代の女性（サンリオ・すみっコぐらし・ハイキュー!!・ちいかわのグッズ好き）。
- **体験の中心:** 一人で回さず、**リンクで友達を呼び、同じルームで同時に開ける**（共有ルーム。上限は画面に出さない）。当たりも、その場でいっしょに見る。
- **ビジネスモデル:** JTCC 型（B2C）。JTCC が仕入れ・検品した在庫を、確率を公開したガチャで販売する。C2C ではない。

画面の設計は、チーム内の設計書「ラストピース アプリ UI 設計書（2026-09-25）」と、その試作（回す画面の試作5・画面設計 2/4 改訂）を移植したもの。

## 2 つの版（限定公開アプリ・一般公開アプリ）

同じ 1 つのアプリ（同じビルド）に 2 つの入口がある（担当者の決定 2026-09-26、#135）。文書・Issue・PR で画面・データ・規則を語るときは、どちらの版の話かを書く。「デモ」「デモ用」「ピッチ用」を単独で使わない。「本番」は本番環境（公開先）の意味だけに使う。用語集と書き方は [`.claude/skills/tech-writing/SKILL.md`](../.claude/skills/tech-writing/SKILL.md)。

| 用語 | 意味 | 対象者 | 入口 | サンリオ IP |
| --- | --- | --- | --- | --- |
| **限定公開アプリ**（ピッチ用） | 関係者だけが使う版。ホストが部屋を作り招待する | 関係者・ピッチの場 | ホスト用リンク（`#/host/<キー>`）・招待リンク | **IP の許諾あり**（キャラクターの絵などを使ってよい。範囲は担当者が Issue で個別に決める） |
| **一般公開アプリ** | LP から誰でも入れる版。毎時 00 分・30 分に自動で始まる部屋（#58。未実装） | 一般の利用者 | LP の固定リンク（例 `#/demo`） | **実写真のサンリオ商品画像だけ**使える。キャラクターのイラスト・ロゴ・アセットは不可 |

- 「キャラクターの絵・サンリオのアセットはアプリ・リポジトリ・プレビューに入れない」の規則は、一般公開アプリと、両方の版で共有される画面・部品・リポジトリの素材に適用する。限定公開アプリだけに出る画面・素材は、担当者の許諾の範囲で使える（どこまで使うかは担当者が Issue で個別に決める）。
- 開発サーバー・単体テスト・E2E で使う**端末内の模擬ルーム**（同じブラウザのタブの間だけで動く模擬サーバー）は、どちらの版でもない。下の「実在のデータや動作を装わない」を見る。

## 決まっていること

| 項目 | 決定 |
| --- | --- |
| 基盤 | React + Vite のスマホ向け Web アプリ（このリポジトリの基盤に合わせる） |
| ホーム画面から開く・触り心地（F16） | ネイティブ化（Capacitor など）はまだしない（担当者の決定 2026-09-26）。Web のまま、ホーム画面に追加するとリンクバーのない全画面（manifest の `display: standalone`、`start_url`・`scope` は相対で最新版・固定版・確認用プレビューのそれぞれの場所）で開く。画面全体のピンチ・ダブルタップの拡大、引っぱって再読み込み、端のバウンドと横スワイプの戻る/進む、押す部品と絵の長押しメニュー・文字選択を止める（`src/index.css`、iOS のピンチと Android の長押しメニューは `src/app/gestures.ts`）。入力欄と本文は選べるまま。文字の拡大は OS の文字サイズに従い、viewport の `maximum-scale`・`user-scalable=no` は使わない。Service Worker・オフライン・プッシュ通知は入れない。「ホーム画面に追加」の案内の画面は Figma のマスターに状態を足してから実装する（未実装。下の未決事項） |
| 呼び方 | 「パック」ではなく「ガチャ」 |
| 作品（上のカード） | サンリオ／すみっコぐらし／ハイキュー!!／ちいかわ |
| グッズの種類（タブ） | すべて・ぬいぐるみ・アパレル・バッグ・アクセサリー・文房具・缶バッジ/アクスタ |
| 並び替え | おすすめ順／即完売品／残りわずか／新入荷 |
| 等級 | 画面に等級の文字（大当たり・中当たり等）は出さない。光り方で見分ける（目玉＝ローズゴールド、キラキラ＝パール）。詳細画面の「✦ 目玉」の見出しだけ使う。読み上げ用に隠し文字で区分を伝える |
| 確率 | 常に公開。**いまの残りから計算した値**を中身ごとに出す |
| 残り | **残り口数の数字（◯/◯）は出さない。** 数字なしの残りバーと言葉（たっぷり／半分くらい／残りわずか／売り切れ）を、ガチャ一覧のカードに出す。ガチャ詳細はマスター `267:8279` に合わせて残りバーを出さない（担当者の指示 2026-09-26「基本的にマスターを正として」、F11） |
| 回し方 | 1回ずつ。**回す前に確認**（必要なデモコイン・所持 → 確定後の残高）を出し、「◯使って1回引く」で確定したときに初めてコインを使って結果を決める。そのあと **3回転**。主の操作は**つまみを指で右に丸くなぞって回す**（1 周＝360° で 1 回転。**右回りだけ進み、左回りは進まない・戻らない**。半周では進まず、速すぎても 1 回に 1 回転まで。指を離しても途中の角度は残る。担当者の要望と決定 2026-09-26、#59 → #114（Figma）・#115（アプリ）。F11 で外した「なぞって回す」を、マスターを先に直して戻した）。つまみのタップと「1タップで1回転」ボタン（キーボード・読み上げ用）でも 1 回転ずつ進める。なぞる要素は `touch-action: none` と `setPointerCapture` で、ページのスクロール・拡大とぶつからず、指が少し外れても続く。押せる範囲はつまみを中心に 160×160（マスターの `Knob drag / 160×160 target`）。回転・開封の途中は戻る・下のタブを出さない。連続で回す機能は作らない |
| 演出 | ひとりで回すときは、筐体はマスターの静止の絵（部品 `T08 / Gacha Machine` `419:10377`、基準 `267:10520`。#114 の v2-D）で、回転しても筐体・背景・ピピは変わらない（担当者の指示 2026-09-26「基本的にマスターを正として」、F11）。変わるのはつまみ（`T08 / Knob v2`）だけ: なぞる前はつまみの上に右回りの短い矢印（turn1〜3）、なぞっている間はつまみが指の角度に追従して回る（dragging。軌跡の線は出さない）、1 周した瞬間は 0.4 秒だけ「カチッ」（click1: つまみの外側の短い線 8 本と右上の白い札、本文「N 回転、できた！」、その分の進み具合バーが `--main` で点灯してから `--primary` に落ち着く）。1 周ごとに対応端末では短い振動（`navigator.vibrate`）、音は既存の設定に従う。動きを減らす設定でも、つまみの追従（操作の結果）とカチッの線・札は出し、アニメだけ止める。3 回転目はカチッではなくカプセルが出る（#86。click3 と capsule-S の見た目は #116）。友達と回す（デモ）は T10 の画面を作るまで以前の演出（背景の豪華化・ピピ・札）を残す。**確定演出は本当に目玉が出るときだけ**（ガセ演出はしない） |
| 開封 | カプセルをタップで開ける。マスター `267:8387`〜`8421` どおり、光り方にかかわらず **いつも3段階**（担当者の指示 2026-09-26「基本的にマスターを正として」、F11）。**カプセルは光り方で3種の見た目**（普通のピンク／キラキラ＝真珠色と小さな星・粒／目玉＝淡いシャンパンゴールドでふんわり光る。点滅・強い発光はしない）。3回転の直後に筐体の受け皿へカプセルが出て、その時点で当たりが分かる（担当者の要望と確認 2026-09-26、#86。マスターの部品 `376:11670`）。結果（`267:8438`）は品・使ったコインと残高・保存先と「街へ戻る」だけを出す（「もう1回まわす」「当てたものを見る」「¥◯◯相当」はやめた。相当額は当てたものの記録とみんなの結果で出す） |
| 友達と回す | 共有ルーム（T10・F05）。ホスト用リンクを持つ人がルームを作り、招待リンクを受け取った人だけが入れる（公開の募集はしない）。抽選か見守りを選び、全員が同じ時刻に開封して、自分の結果とみんなの結果を見る。上限（100人）は画面に出さない |
| コイン | **1コイン＝1円**。購入は架空の操作（押すと増えるだけ。決済は作らない）。はじめての人には体験用 30,000 コイン（架空の通貨なので大量に配る。担当者の要望、2026-09-26） |
| 当てたもの | 倉庫ではなく記録。1つずつ「届けてもらう」か「コインに交換」 |
| コインに交換 | **参考価格の一律 20%**（端数切り捨て）。出金不可・取り消し不可。画面では「換金」と言わない |
| 案内役 | オリジナルキャラクター「ピピ」。実在キャラクターにはしゃべらせない |
| 見た目 | 配色「さくらミルク」（白〜パステルピンク）。演出時だけローズゴールドとパール。確定時はワインローズ。ロゴはリボン案 |
| 画像 | `public/assets/` のオリジナル生成素材だけを使う（ロゴ・作品・種類・UIアイコン・商品イラスト・バナー） |
| 声と音 | ピピの読み上げとチャイム・振動は、マイページで ON にしたときだけ鳴らす（最初は OFF） |
| 保存 | この端末の localStorage（キー `lastpiece_app_v1`）。壊れたデータは最初の状態に戻す。保存できない端末ではその旨を表示。F04 でお気に入りと「いいな〜」を追加（前の保存データは空として読む） |
| 棚（F04・F11） | **棚の枠＝1つのガチャの中身の種類**（マスター `267:8476` の9枠はサンリオ カプセルミックスの中身9種）（担当者の指示 2026-09-26「基本的にマスターを正として」、F11）。棚に出すのは、いちばん最近に回したガチャ（まだ回していなければサンリオ カプセルミックス）。枠の数と並びはそのガチャの中身と同じ（8〜12種）。持っている種類（手元にある・届け待ち）は絵と「持っている」、まだの種類は「＋」「未入手」と中身の名前。コインに交換した品は外れる。ほかのガチャの品は棚の外（当てたものの記録で見られる）。棚の中だけを縦にスクロールする |
| お気に入り（F04） | 持っている品に付け、いつでも外せる。枠は種類ごとに決まっているので、付けても位置は変わらない（F11） |
| フレンド（F04） | いっしょに回した友だち（説明用の架空データのゆい・さき）だけが出る。ゆいの棚は固定の架空データ、さきは「今は見られません」の状態見本。友だちの品は自分の持ち物に入らない |
| 共有（F04） | 「友だちからの見え方」は確認だけで、どこにも公開しない（「共有はまだ実行していません」）。見えるのはプレビューに出る棚の9枠だけ |
| 表示 | 各画面の上部の1行は「ラストピースは開発中のサービスです」（マスター T07〜T10）。すべての入口の下の注記は「ラストピースは開発中のサービスです。商品・価格・在庫は例です。各権利者とは関係ありません。」。「提案モック」という言葉は使わない（担当者の決定 2026-09-26、[#69](https://github.com/doc-gif/jtcc-group-e/issues/69)）。「公式サービスではありません」は使ってよい。免責は、実サービスでないことが伝われば足りる（担当者、2026-09-26） |

## 共有ルーム（決定済み・2026-09-26、F07・F10・F13・#88）

提案の場で使う共有ルーム（[契約](SHARED_OPENING_CONTRACT.md)）の決定。画面は Figma の後に実装する（今は状態層と SQL だけ）。

| 項目 | 決定 |
| --- | --- |
| 人数 | ホストを含め最大 100 人（内部の上限）。画面には上限を出さず「N人が集まっています」だけを出す |
| 架空在庫 | 新しいルームは 300 個（目玉 30・ポーチ 90・缶バッジ 180。以前の 100 個と同じ比率で 3 倍） |
| 予約開始 | ホストが「1 / 3 / 5 / 10 分後に開始」を選ぶ。全員に開始時刻と秒読みを出す。開始前ならホストは変更・取り消しでき、「今すぐ開始」も使える。時刻になると、ホストが不在でもサーバーが開始する（その時点でオンラインの準備OKの人が対象）。予約はルームの期限（作成から 2 時間）の 1 分前まで。誰も準備していなかったなどで始まらなかったときは、ホストにその理由を出す |
| ピッチモード（ホストモード） | 限定公開アプリの機能。対面のビジネスピッチで会場が反応できるよう、ホストがルームごとにオンにする。オンの間、各ラウンドで参加者 1 人に目玉を確定する（本物の在庫から出す）。全員の画面に「ピッチ用デモ：このラウンドは1人に目玉確定」などと明示する（画面の文言。ピッチモードがオンだと分かる文にする）。目玉の在庫が 0 なら装わず、通常の抽選にして「確定なし」と分かるようにする |
| 確率の表示 | ピッチモード中は、公開している確率は確定枠には当てはまらない（確定枠以外の人には当てはまる）。画面はそのことを確率の近くに表示する |
| ホスト（F10） | ホストになれるのは担当者だけ。リンクは2種類で、参加者に配る招待リンクと、担当者だけが持つホスト用リンク（秘密のキー入り）。ホスト用リンクでだけルームを作れ、別の端末で開けばホストに戻れる（ほかの人の席・コイン・結果はそのまま）。参加者がホストを代わることはない（旧仕様の「ホスト交代」は廃止）。ホストがいなくても予約した開始は時刻に始まる。無効なリンクは「ホスト用リンクが無効」と案内し、ルームは作らない |
| 名前（F10） | 1〜12文字。ルームの中で重ならない（全角・半角、大文字・小文字、空白の違いは同じ名前とみなす）。名前を選ばずに入ると「ゲスト さくら12」のような名前が付き、入る前に見せる。使われている名前には「「もも2」で入る」のように空いている候補を出す。ロビーで名前を変えられる |
| 開始の人数（F13） | 担当者の決定（2026-09-26「ガチャはホスト以外に2人以上じゃないと引けないようにしたい」）。ルームのラウンドは、ホストのほかに退室していない参加者が2人以上（ホストを含めて3人以上）いるときだけ始められる。足りないと「今すぐ開始」は押せず「あと N 人で始められます」と出す（参加者の画面にも同じ案内）。予約は人数が足りなくてもでき、時刻を過ぎても足りなければ始めずに待ち、2人目が入った時点で始まる（ホストは待っている予約を変更・取り消しできる）。ピッチモードも同じ。人数は「集まっている人 N人」だけを出し、上限は出さない。ひとりで回すガチャと「友達と回す（デモ）」（本人と友だち2人）は変わらない |
| 開ける順序（#88） | 担当者の決定（2026-09-26）。秒読みの後、抽選に入った人はまず自分の画面で自分のカプセルを開け、自分の結果だけを見る。開けるまで結果は端末に送らない。抽選に入った人が全員開けたら、全員の結果を見られる。開けない人がいても、カプセルが出てから 10 秒で全員の結果に進む。開けないまま退室した人は待たない。見守る人にはカプセルを出さず、全員の結果だけを見せる。中身は開始の時点で決まっていて、開ける順番では変わらない。予約の期限（ルーム終了の 1 分前まで）は変えない |

「確定演出は本当に目玉が出るときだけ」は変わらない。確定枠の目玉は実際に出る。

## 守ること（法務・ブランド）と、効かせ方

`scripts/governance.test.mjs`（`pnpm test` で実行・CI で落ちる）と、`src/domain/domain.test.ts` で確認する。

- 禁止語（換金・必ず当たる・還元率100%・大当たり・中当たり）を画面の文言に使わない。
- 各画面の上部に「ラストピースは開発中のサービスです」、すべての入口の下に「商品・価格・在庫は例です」の注記を出す。「提案モック」は画面・メタ情報・README に使わない（#69）。「公式サービスではありません」は使ってよい（実サービスでないことが伝わればよい、担当者 2026-09-26）。
- ピピのセリフ（`src/copy/pipi.ts`）に実在キャラクターの名前を入れない。
- 公開する画像はオリジナル素材の置き場所だけ（許可リスト）。サンリオ等の公式画像・ロゴ・キャラクター絵は、一般公開アプリと、両方の版で共有される画面・部品・リポジトリの素材に使わない。限定公開アプリだけに出る画面・素材は担当者の許諾の範囲で使える（#135。置き場所はリポジトリの外。実物グッズの写真は非公開の Storage、[PITCH_PHOTOS.md](PITCH_PHOTOS.md)）。キャラクター名は取り扱い商品の事実としてテキストだけ。
- 交換率は一律 20%。交換シートに「出金できません」「取り消せません」を出す。
- ガチャ詳細に残り口数の数字を出さない。確率の合計は 100%、残り 0 は 0%。
- 確定演出は目玉のときだけ（`shouldShowKakutei`）。
- 発売から 90 日以内の商品はカタログに入れない（`releasedAt` をテスト）。
- 友達へのコイン送付、賭け・勝ち負けの表示、「売る」機能は作らない。

## 画面（ハッシュで表す。固定版 URL でも再読み込みできる）

| URL | 画面 |
| --- | --- |
| `#/` | 街（ホーム）。URL にハッシュがない状態で開いたときだけ導入（約3秒・スキップ可、動きを減らす設定では1枚で自動では進まない）を出し、終わると `#/` に置き換える。街の絵がまだ準備できていなければ読み込み中を出す（F09） |
| `#/gacha` | ガチャのお店（ガチャ一覧） |
| `#/welcome` | はじめて（説明・ニックネーム・ホーム画面への追加の案内） |
| `#/gacha/<id>` | ガチャ詳細（マスター `267:8279`：目玉の候補といまの確率・1回のデモコインと引いた後の残高・「中身N種と確率を見る」「1回引く準備へ」。ルームの入口「友達と回す」も残す） |
| `#/gacha/<id>/odds` | 中身と確率（全種の確率を中身の順に表で出す。値はいまの残りから計算し、表示だけ小数第1位で合計100.0%に端数調整。F03・F11） |
| `#/room/new` | 共有ルームを作る（ホスト用キーを持つ人。端末内の模擬ルームでは、この端末をホストにして作れる）。以前の `#/gacha/<id>/room` もここへ |
| `#/gacha/<id>/spin` | ひとりで回す（回す前の確認 → 3回転 → 3段階の開封 → 今回の結果。売り切れ・コイン不足のときは理由と「ガチャ一覧に戻る」だけを出し、回さない） |
| `#/room/<招待>` | 共有ルーム（T10 の28画面）。招待 → 名前 → ロビー（抽選か見守り）→ 準備 → 秒読み → 同時開封 → 自分の結果・みんなの結果。ホストも同じ URL で、画面はサーバーの状態で決まる。再読み込みすると同じルームへ戻る |
| `#/host/<キー>` | ホスト用リンク（F10）。キーをこの端末に保存してアドレスバーから消し、開いているルームのホストに戻る。なければルームを作る。違うキーは「ホスト用リンクが無効」 |
| `#/together` | フレンド（いっしょに回した友だち（説明用の架空データ）の棚への入口）。ルームはガチャ詳細の「友達と回す」から作る |
| `#/friend/<id>` | 友だち（説明用の架空データ）の棚。見られない友だちは理由と戻り先を出す |
| `#/friend/<id>/<品のid>` | 友だちの1点と「いいな〜」（この端末に記録するだけ・同じ品に1回だけ） |
| `#/shelf` | わたしの棚（最後に回したガチャの中身の種類の枠） |
| `#/shelf/<品のid>` | わたしの1点（お気に入りを付ける・外す） |
| `#/shelf/share` | 友だちからの見え方（共有前のプレビュー。公開はしない） |
| `#/collection` | 当てたもの（上はマスター `267:8623` の棚と同じ枠の一覧で、持っている品を先に。下に当てた記録：合計・絞り込み・コインに交換・届けてもらう。棚の「一覧」から来る） |
| `#/me` | マイページ（コイン・ニックネーム・声と音・お知らせ・きまり・審査用の操作「デモ操作」） |

下のタブはデザインマスターに合わせて **街・ガチャ・コレクション・フレンド** の4つ（F02）。コレクション → `#/shelf`（わたしの棚）、フレンド → `#/together`（フレンド）へつなぐ（F04）。街の地図の「コレクション」も棚へ行く。マイページはタブにせず、上部のコイン残高とアイコンから開く。

街の地図はマスターの部品（`267:10063`）のオリジナルのベクター画をコードの SVG で描き直したもの。マスター中央の内部キャラクター参照は案内役ピピに、注目のピースの商品写真は `public/assets/goods/` のオリジナルの商品イラストに置き換えた（内部素材は使わない）。地図の「当てたもの N」は、この端末の記録の数（マスターの「棚 2/9」は説明用の架空データのため出さない）。

## 読み込み中（T07 Loading `267:8244`、F09）

- 街を出す前に待つのは、上部のコイン・マイページのアイコンと注目のピースの絵、使っている文字（フォント）だけ。街の地図はアプリに組み込んだ SVG なので待たない。読み込みはアプリを開いた直後に、どの画面から開いても始める。
- 待ちが 0.3 秒（`LOADING_DELAY`）続いたときだけ読み込み中を出す。それより早く準備できたら出さない（ちらつき防止）。わざと待たせる最低表示時間はない。導入のあとは、猶予のあいだ導入を出したままにする。
- 読み込み中は動きのない1枚。見出し「街の準備をしています」と文で状態を伝え、進み具合の数字は出さない（本当の割合が分からないため）。準備できたら自動で街へ進む。「スキップ」「街を見る」で先に街へ行ける（絵はあとから出る）。
- 読み込めなかったら「街の準備ができませんでした」と伝え、「もう一度読み込む」でやり直せる。先に街へも行ける。
- **Figma 修正待ち → F12 で解決:** マスターの2文目「通信が必要な操作は、準備ができたらお知らせします。」は、アプリに通信が必要な操作もお知らせの仕組みもないため、実際の動きどおり「準備ができたら、そのまま街へ進みます。」にした。担当者の承認（2026-09-26「マスターにたしてください」）で、マスター `267:8244` の文をこれに変え、失敗とやり直しの状態を `316:6490` として追加した（[ADOPTED_DESIGN.md](ADOPTED_DESIGN.md)）。

## 実在のデータや動作を装わない（説明用の架空データと端末内の模擬ルーム）

- 共有ルームは、本番・確認用プレビューのビルドでは専用の Supabase（`lastpiece-pitch`）につながり、別々のスマホで同じルームに入れる（F15、担当者の依頼 2026-09-26「みんなで開封する機能を複数の端末でできるように」。[記録](MULTI_DEVICE.md)）。コイン・商品・在庫は説明用の架空データのまま。Supabase の設定がない開発サーバー・単体テストと、E2E（`lastpiece_room_force_demo`）では、同じブラウザのタブの間だけで動く端末内の模擬ルーム（模擬サーバーを localStorage に置く。どちらの版でもない）。別のタブで招待リンクを開くと別の参加者として試せる。画面に「デモ：この端末のブラウザの中だけのルームです（別のスマホとはつながりません）」と出す（画面の文言）。以前の「友達（ゆい・さき）といっしょに回す」再現はなくした（F05）。
- 限定公開アプリの実物グッズ写真（F15、担当者の許諾 2026-09-26「ピッチ・関係者だけ」「実物グッズの写真だけ」。一般公開アプリでも実写真のサンリオ商品画像だけは使える（#135 の用語の表）。出す時期は #58 の子 Issue で決める）: Supabase につないだルームの結果画面で、自分が当てた品の絵を実物の写真にし、下に権利表記の1行を出す。写真は担当者が非公開の Storage に置き（[手順](PITCH_PHOTOS.md)）、開いているルームの参加者とホストだけが読める。写真がない・読めないときはオリジナルの商品イラストのまま。端末内の模擬ルームでは使わない。賞品名・在庫・確率は説明用の架空データのまま（実物の販売・在庫・当選を表さない）。
- フレンドの棚（ゆい）・「今は見られません」（さき）・「いいな〜」は説明用の架空データ。「いいな〜」はこの端末に記録するだけで、相手には届かない。棚の共有（公開）はしない。
- カタログ（4作品 × 3本 = 12本）・参考価格・在庫はすべて説明用の架空データ。
- コインの購入・発送はしない。「届けてもらう」は状態を「届け待ち」にするだけ。
- マイページの「次の1回を目玉確定にする」は審査用の操作（画面の見出しは「デモ操作」。1回で解除）。

## 設計書との違い（このリポジトリで判断したこと）

- 設計書は Next.js（`lastpiece-app/`）を想定していたが、このリポジトリの基盤（React + Vite・GitHub Pages）に合わせた。
- 画面の URL はハッシュ（`#/...`）にした。静的ホスティングの固定版 URL でも再読み込みできるようにするため。
- ホームのカードの「♡ ◯ルームで回してる」は出していない。実際のルーム数がないため（数字を装わない）。
- カタログは 1本あたり 8〜12種（設計書は 12〜24種）。

## チームの基盤ルール（main で決まっていること）

- React + Vite を基本とし、スマートフォンで利用する Web アプリを想定する。AI を活用して継続的に開発する。
- 本番の GitHub Pages は明示指示時のみ公開し、各バージョンを固定 URL と Release で残す。確認用サイトは [自動プレビュー](PREVIEW.md) として別途配布する。
- PR ベースで開発し、機能テストと UI/UX 基準の成功を自動承認・マージの条件にする。
- Apple HIG の Web に適用できる原則と、キャラクター／女性向けゲームの設計事例を UI/UX 基準に使う。
- デザインは指定 Figma ファイルで管理する。ラストピースの画面は、担当者の指示（2026-09-26）によりピンク基調の「さくらミルク」を正とし、[デザインマスター](ADOPTED_DESIGN.md)にまとめた。JTCC App 初版の緑系（`docs/DESIGN.md`）は雛形の記録で、ラストピースの主操作には使わない。

## 未決事項

- **ホーム画面のアプリでルームに入る方法（F16 の続き）:** ホーム画面のアプリにはアドレスバーがなく、招待リンク・ホスト用リンク（`#/room/…`・`#/host/…`）を開くとブラウザのタブで開く。iPhone ではホーム画面のアプリと Safari の保存データ（コイン・名前・匿名 ID・ルームの参加）も別になる。そのため今は、ルームはブラウザで入る。ピッチで「ホーム画面に追加してから入る」と案内するなら、招待リンクを開いた画面で追加したときにその招待で開けるか、アプリの中で招待コードを入れられるかを、「ホーム画面に追加」の案内（Figma のマスターが先）と一緒に決める。
- **配色（決定済み・2026-09-26）:** ピンク基調の「さくらミルク」を正とし、緑アクセントの版は不採用。主操作は `#be6482` のまま、ボタンの文字を 19px 太字（WCAG の大きい文字、白と 3.94:1 で 3:1 以上）にする。19px 未満のピンクの文字はワイン `#a94a68`（白と 5.43:1）。淡いミント `#E3F1E7`（タブの選択背景、コイン札、T10 の舞台背景）は残す。本文・補助文はマスターの `#172324`・`#52615a`（担当者の判断 2026-09-26、マスターを先に修正、アプリは F06 で反映。[DESIGN.md](DESIGN.md)）。
- **マスターの Web 実装（一部）:** T07 の街ホーム・導入・動きを減らす設定と、共通の4タブは F02 で実装した。T07 の読み込み中（`267:8244`）は F09 で実装した（街の絵が本当にまだ準備できていないときだけ出す。下の「読み込み中」）。T09 の棚・フレンド12状態は F04 で実装した（内部の商品写真はオリジナルの商品イラストに置き換えた）。色は担当者の判断（2026-09-26）で決まり、マスターを先に直した（主操作 `#be6482`＋19px 太字、小さい文字はワイン `#a94a68`、ミント `#E3F1E7` はそのまま。[変更履歴](ADOPTED_DESIGN.md)）。アプリへの反映は F06 で行った（`src/index.css` のトークンと共通のボタン・タブ）。T08 のガチャの流れ（一覧・詳細・確率・回す前の確認・3回転・3段階の開封・結果・売り切れ・コイン不足）は F03 で実装し、F11 でマスターとの違いをアプリ側で解消した（担当者の指示 2026-09-26「基本的にマスターを正として」。残す例外は [ADOPTED_DESIGN.md](ADOPTED_DESIGN.md)）。T10（ルーム）の28画面（F13 の開始の人数を含む）は F05 で実装した（マスターとの違いは [ADOPTED_DESIGN.md](ADOPTED_DESIGN.md) の「T10 の Figma 修正待ち」）。ルームの上限は 100 人（ホスト込み）にし、画面には出さない。
- **フレンドの友だち（F05 で未決）:** フレンドの友だち（説明用の架空データのゆい・さき）は、以前の Figma の画面「友達と回す（デモ）」の記録から出ていた。その再現をなくしたため、新しい利用者のフレンドは空のまま。共有ルームでいっしょに開けた人をフレンドにするかは担当者の判断待ち。
- **棚の枠と友だちの共有範囲:** 棚の枠は1つのガチャの中身の種類（担当者の決定 2026-09-26）。空き枠はマスターどおり中身の名前を出す。棚を見せる相手の選び方・招待・本当の共有は未決（T10 以降）。
- **T08・T09 のマスターとの違い（F11 で整理）:** 担当者の指示（2026-09-26「基本的にマスターを正として」）で、F03・F04 の「Figma 修正待ち」はアプリ側をマスターに合わせた。残すのは守ること（内部写真を使わない・開発中のサービスである注記・確率を正直に出す・ガセの確定演出をしない）による例外と、既存の決定を守るために残した状態（ルームの入口、交換・届けてもらう、ガチャごとに違う中身の数など）だけで、[ADOPTED_DESIGN.md](ADOPTED_DESIGN.md) に理由とともに列挙。後者の5点（F11 で「Figma 修正待ち」）は、担当者の承認（2026-09-26「マスターにたしてください」）で F12 にマスターへ追加して解決した（詳細の友達と回す `316:6962`、当てた記録 `316:8044`、お気に入りをやめる `316:7877`、棚の中身の1行と10種の棚 `316:7943`・棚の外 `316:7911`、友達と回すの演出 `316:7005`・`316:7435`）。配色（主操作・小さいピンク文字・タブのミント）は F06 で共通トークンを切り替えて Web をマスターに合わせた。

- ルームの同期の速さ。#68 で Supabase Realtime の起床通知を有効にし、入室・準備・「今すぐ開始」などがほかの端末に数秒で届く（ポーリング 15 秒は保険として残す）。未決: 無料プランの Realtime の上限（同時接続 200・イベント 100 件/秒、配信 1 件ずつを数える）で 100 人のルームが足りるか（プランの変更か、通知をまとめるか。担当者の判断、#68）。
- 招待の「QRを見せる」、みんなの結果を「画像で保存・シェア」。
- 発送（届け先の入力）、決済・前払式支払手段の届出への対応、特商法・資金決済法の表示の中身。
- 抽選をサーバー側に移すか（実サービスにする段階）。
- 権利者（サンリオ等）との関係、取り扱い商品名の表示ルールの最終確認。
- 本番公開（GitHub Pages）のタイミング。本番は明示指示まで更新しない。確認用プレビューは CI 成功後に自動配布する。
- アクセス解析の要否。

## 次の機能を決めるときのテンプレート

### 課題

誰が、どの場面で、何に困っているか。

### 利用者の操作

開始から完了までの操作を短く書く。

### 受け入れ条件

- 利用者が確認できる結果を列挙する。
- エラー時や空状態の表示も定義する。

### 決定事項と保留事項

決めた理由と、後で判断する項目を記録する。
