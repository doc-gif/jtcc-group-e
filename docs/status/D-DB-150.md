# D-DB-150 限定公開アプリのキャラクター素材の非公開バケットと読み取りの権限

## 成果
#144 の案 D（2-1）の置き場を Supabase `lastpiece-pitch` に作った。非公開バケット `pitch-characters`（PNG・WebP、300 KB まで）、関数 `lp_can_view_characters()`（開いているルームのホストか active な参加者だけ true）、`storage.objects` の SELECT ポリシー `lp_pitch_characters_read`。追加だけで、既存の表・バケット・ポリシー・`lp_host_keys` は変えていない。夜間モード（2026-09-26）の依頼。

## 検証と証拠
- migration `20260926172325_lp_pitch_characters` を 2026-09-26 17:23 UTC に適用。`list_migrations` の version とファイル名が同じ。
- PGlite の `scripts/shared-db.test.mjs` に 3 件（読める人・読めない人・書けない）。
- 実 DB の権限・読み取りの動作（取り消す取引）・advisors の結果は [SQL_MIGRATION_144.md](../SQL_MIGRATION_144.md)。
- DB に先に適用済みだった #88 の `20260926133919_lp_open_self_paced` のファイルを、#130 のブランチから変えずに入れた（理由は同じ文書）。

## 残課題
- 担当者: 素材 7 枚のアップロード（#144 の 2-6・2-7）。
- 担当者の判断待ち（#150）: 別の開いているルームの参加者も読めること、`lp_open_self_paced` のファイルを先に入れたこと。
- アプリ側の取得は #149、画面は #146。#58 で `kind` を足すときに `lp_can_view_characters` を `kind='pitch'` に絞る。

## ブランチ・記録
`task/150-pitch-characters-bucket`、#150、PR #153、ロック #151。
