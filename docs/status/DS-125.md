# DS-125 #122 の DS 追加分のトークンをアプリに反映

## 成果
#120 の担当者の決定（B1・B3・C6・D2・E2・F5、2026-09-26）で #122 が Figma の Lastpiece v2 DS に足した変数 15・文字スタイル 5・色/効果スタイル 5 を、同じ名前・同じ値で `src/index.css` と `design-system/tokens.css`（v0.5）に持った。`--radius` は `radius/control` の別名にして 14px → 12px（#120 D8）。`action/pressed` はワイン（`--lp-v2-action-pressed` → `--deep`）。同じ値の直値（`#6B2B45`・`#EFE3E7`/`#6E5A61`・カプセルの光・棚の角丸）はトークン参照に置き換えた。見た目で変わるのは `var(--radius)` の 10 か所の角丸だけ。

## 検証と証拠
`scripts/design-tokens.test.mjs`（tokens.css と index.css の同名トークンの一致、#122 の追加分の有無、`--radius` 12px）。Figma の変数 ID・WEB 名・値は `use_figma` の読み取りで確認（2026-09-27 02:30 UTC 頃、変数 `456:2`〜`456:16`・`177:41`・`177:25`）。レビュー記録は `docs/ux-reviews/issue-125-ds-tokens.json`。PR #158。

## 残課題
見出しの Zen Maru への切り替え、コイン札の白地＋melody-pink 枠（B1）、`.page-header-notice` 14px、Chip・Input・Text ボタンの寸法、棚の一覧の行の角丸（Web 18・Figma 16）、`:active` は #143。`border/focus` のワイン化（F4）は #123 の後。

## ブランチ・記録
`claude/issue-125-ds-tokens`、#125、PR #158。
