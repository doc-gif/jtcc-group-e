# AI 開発ガイド

このリポジトリは、スマートフォンで使う React + Vite の Web アプリです。仕様が未確定の領域は推測で実装せず、`docs/PRODUCT.md` に決定事項と未決事項を記録してください。

最新の進捗と次候補は [docs/STATUS.md](docs/STATUS.md) を読む。タスク完了ごとに `docs/status/<ID>.md` を足して PR へ push する（下の「共有資源のロックと担当の宣言」）。ただし main・本番・PR・ブランチ・DB の**今の状態**は文書ではなく `node scripts/live-state.mjs` で確かめる（下の「情報の鮮度」）。引き継ぎは担当者が明示希望した場合だけ [docs/HANDOFF.md](docs/HANDOFF.md) に従い、通常の依頼を優先する。

## 作業の進め方

1. 変更前に `README.md`、`docs/PRODUCT.md`、`docs/DEVELOPMENT.md`、`docs/TESTING.md`、`docs/UI_UX_STANDARDS.md`、`docs/DESIGN.md` を読む。
2. 目的、利用者への影響、受け入れ条件を短く整理する。仕様が曖昧でも進められる部分を先に進める。
3. 変更を小さくまとめ、画面・状態・データ取得の責務を分ける。新しい依存は必要性を説明できるときだけ追加する。
4. モバイル幅 320px から確認し、タップ領域、キーボード操作、ラベル、コントラスト、セーフエリアを考慮する。
5. UI を変更したら `docs/UI_UX_STANDARDS.md` に従い実画面を確認し、現在の UI に対応するレビュー記録を追加する。ローカルは影響範囲を検証し、CI で `pnpm verify` 相当の全検証を必須にする。失敗した場合は修正し、原因と未解決点を報告する。
6. 変更内容、検証結果、残る判断事項を報告する。
7. 複数のエージェントが同時に作業するときは [docs/OPERATIONS.md](docs/OPERATIONS.md) に従う。担当の Issue に着手コメントと `in-progress`、PR 本文の先頭に `Closes #N`（1 Issue に PR が複数あるときは最後の PR だけ `Closes`、それ以外は `Refs #N`）、共有資源は `lock:*` の Issue で取る。ロックの期限は15分、延長はロック Issue へのコメント、解放後は前の保持者と話してから使う。タスクの状況は文書に書き写さず Issue とラベルに置く。

## 情報の鮮度（古い記録で動かない）

複数のエージェントと担当者が同時に動くので、文書に書いた「今の状態」はすぐ古くなる。古い記録を信じて、公開済みの版を公開し直す・消したキーを消しに行く・マージ済みの PR を待つ、を起こさない。

- **変わる事実は文書に書き写さない。** main の SHA、本番の版、Release、PR・CI の状態、ブランチ、DB に適用した migration、Figma マスターの今の状態は、文書に「現在〜」と書かない。書くのは確認方法（`node scripts/live-state.mjs`、Supabase MCP の `list_migrations`、Figma の node）。STATUS.md・HANDOFF.md に残すのは、決定・理由・未決事項・担当者の返事待ち・やってはいけないこと・完了の記録（「#41 でマージ」のように過去の出来事として）。`scripts/live-state.test.mjs` が書き写しを検査する。
- **動く前に実態を確かめる。** 作業の開始時、STATUS.md・HANDOFF.md・引き継ぎの指示に書かれたことをもとに動く前、本番公開・DB への適用・Figma マスターの変更の前に `node scripts/live-state.mjs` を実行する（DB は `list_migrations` も）。記録と実態が違えば実態を正とし、差を利用者に伝え、同じ PR で記録を直す（終わった項目は消す）。
- **時点の事実を書くときは時刻を付ける。** 例外として時点の事実を残すとき（証拠・調査の結果）は「（2026-09-26 06:25 UTC 時点）」を付け、現在形で書かない。
- **PR の外の操作は、その場で記録まで終える。** 本番公開、DB への migration の適用、Figma マスターの変更、ホスト用キーの登録・無効化をしたら、同じ作業の中で記録を直す。DB に適用した migration のファイルは、その日のうちに main へ入れる（DB をリポジトリより先に進めたまま離れない。`live-state.mjs` の「main にない migration」で見つかる）。
- **引き継ぎの指示書は、書いた時点の予定として読む。** 書かれた「やること」がもう終わっていないか、実行中の人がいないかを `live-state.mjs` で確かめてから始める。

## 共有資源のロックと担当の宣言（並列作業）

いくつものエージェントが同時に動く。同じ場所を黙って触らないように着手時に担当を宣言し、同時に触ると壊れる資源は1つの作業ずつ使う。進め方（着手コメント・ラベル・`lock:*` の Issue・Ready 後の見届け）の正は [docs/OPERATIONS.md](docs/OPERATIONS.md) の「作業者（各チャット）の手順」で、ここには壊れる資源と壊さないための決まりを書く。すべてのエージェントが同じ GitHub アカウントで書き込むので、コメントや Issue の先頭には**チャット名とブランチ**を書く。

- **担当の宣言**: 着手コメント（OPERATIONS.md の手順 2）には、チャット名・ブランチに加えて**触るもの**（ファイル・文書の表・Figma node・DB）と持つロックを書く。PR 本文の先頭の「担当の宣言」の欄（テンプレート）にも同じことを書く。
- **着手の前に確かめる**: `in-progress` の Issue、ほかの Draft PR の宣言と、`node scripts/live-state.mjs` の「共有資源のロック」を見る。触るものが重なる相手がいたら、始める前にその Issue で調整する。他のチャットのブランチに push しない。
- **ロック**: 次の資源は、ラベルを付けた Issue（OPERATIONS.md のラベル表）を立てて持つ。Issue には対象・担当・ブランチ・期間を書く。持ち方の細かい決まり（期限・延長・解放後の引き継ぎ）は OPERATIONS.md に従う。

  | ラベル | 対象 | 持つ期間 |
  | --- | --- | --- |
  | `lock:supabase` | Supabase `lastpiece-pitch` への migration の適用、DB・認証の設定 | 適用、ファイル名を DB の version に合わせる、PR のマージまで（PR の本文に `Closes #<ロックの Issue>` と書くとマージで閉じる） |
  | `lock:figma-master` | Figma のデザインマスター `267:8198`、共通変数、本体部品 | マスターの編集と `docs/ADOPTED_DESIGN.md` の記録まで |

  - 開いたロックがあれば、その資源は待ち、ほかの部分（Town v2 の自分の作業領域 `task/<branch>`、コード、テスト）を先に進める。
  - 2つ同時に立ったら、**番号の小さい Issue が持つ**。後の方はラベルを外して待つ（`live-state.mjs` が注意を出す）。
  - 終わったらその場で閉じる。中断して離れるときも、理由を書いて閉じる（持ったまま離れない）。
- **migration の順番**: ファイル名 `<version>_<name>.sql` の version は DB に適用した version と同じにする。main にある migration は書き換え・削除・改名しない（直すときは新しい migration を足す）。新しい migration は main の最新より後の version になる。どちらも `scripts/shared-resources.test.mjs` が CI で検査する。順番の違反は、ロックを持たずに別の作業と同時に適用した印なので、担当者に相談する。
- **共通の表**: 各タスクの完了の記録は `docs/status/<ID>.md`（1タスク1ファイル）に書き、STATUS.md の表には足さない（同じ表の行で衝突しないため）。`docs/PRODUCT.md`・`docs/ADOPTED_DESIGN.md` などの同じ表を変えるときは、担当の宣言に書く。main の取り込みで衝突したら、両方の行を残して解消する。

## コード規約

- TypeScript の strict 設定を維持し、`any` は避ける。
- UI 文言は日本語を基本とし、意味のある HTML 要素を使う。
- 秘密情報を `src/` や `VITE_` で始まる環境変数に入れない。`VITE_` の値はブラウザに公開される。
- 実サービスの API や認証が決まるまでは、実在するデータや動作を装わない。
- デザインはまずモバイルを基準にし、広い画面へ拡張する。

## 完了条件

- 受け入れ条件を満たす。
- 現在の SHA の必須 CI（`pnpm verify` 相当）が成功する。CI が使えない場合はローカルで `pnpm verify` を行い、マージは保護ルールに従う。
- UI を変更したら小さい画面で目視確認する。
- 仕様や実行手順が変わったらドキュメントを更新する。
- 通常の実装・修正依頼は、PR の作成だけで終えず、検証・Draft 解除・Bot によるマージ完了・確認用プレビューの動作確認まで進める。利用者が「調査だけ」「Draft で止める」「マージしない」などと指定した場合は、その範囲に従う。
- 利用者へ確認用 URL、変更点、確認してほしい操作を簡潔に報告する。CI・権限・競合などで完了できない場合は、原因と未完了の段階を明記する。

## デザインの管理先

- 指定 Figma ファイル `yeDF1BwhrxpXI57Daainle` をアプリのデザイン管理先とする。ラストピースの画面の正は [デザインマスター `267:8198`](https://www.figma.com/design/yeDF1BwhrxpXI57Daainle?node-id=267-8198)。画面・状態と node の対応は [`docs/ADOPTED_DESIGN.md`](docs/ADOPTED_DESIGN.md)。緑アクセントの `252:2` は不採用なので元にしない。詳細・各部品のリンクは `docs/DESIGN.md`。
- **デザインは Figma が先、アプリが後。** 見た目・画面構成・状態の変更は、まず Figma のマスターを直し、その Figma に合わせてアプリを実装する（担当者の指示、2026-09-26）。実装中に Figma と違う形にしたくなったら（色のコントラスト不足などの基準違反を含む）、アプリだけで変えずに、Figma の修正案を担当者に確認して Figma を先に直す。アプリだけの「許容差分」を新しく作らない。Figma の修正待ちの差分は PR に「Figma 修正待ち」と明記し、完了扱いにしない。
- 新しい UI は Figma に画面と状態を記録してから実装する。配色はピンク基調の「さくらミルク」とし、主操作を緑にしない。マスターと同じ部品インスタンスを使う。既存のピッチ資料をアプリ仕様として確定扱いしない。
- Figma の作業領域も `task/<branch>` ごとに分け、担当・関連 PR・状態を記載する。共通変数と本体部品は変更前に他の Draft PR と調整し、別の作業領域を上書きしない。
- UI の PR に対象 node URL、確認した状態、変更前後の画像と Figma 照合結果を添える。実画面を確認し、既存の全10項目のレビューで、特に GAME-01 にデザインとの対応を記録する。
- トークンを変えたら `design-system/tokens.css` と `design-system/figma-manifest.json` を同じ PR で更新する。これらは初版時点では参照用で、アプリに自動反映されない。React 部品はまだ未実装なので、存在しない実装や Code Connect 対応を装わない。
- Figma にアクセスできない場合は参照した画像・仕様と未確認箇所を記録し、照合済みと記載しない。Figma のライブ URL は過去デザインを固定しないため、採用版は node と状態の対応を Git commit で残す。内部素材を含む画像は公開 PR に置かない。

## PR・自動承認

- 1タスク・1ブランチ・1worktree。新規作業は最新の `origin/main` から開始する。共有 worktree、main への直接 push、保護を迂回したマージは禁止。
- 作業中は Draft PR。Ready にすると `Quality gate` と `UI/UX gate` の成功後、Bot が現在の SHA を承認して squash merge する。
- 作業者自身が影響範囲の検証と必要なレビューを完了し、PR を Ready に変更する。GitHub CLI なら `gh pr ready <番号>`。マージ許可を毎回聞き直さず、必須 CI と Bot のマージ完了まで追跡する。失敗は修正して再検証する。保護ルールを外さない。
- Ready で CI が成功した後に main が進んだ場合は、Bot の**マージのキュー**が1本ずつ main を取り込み、CI を回してマージする。自分で取り込み直さない。キューから外れた（競合・取り込み後の CI の失敗）と Bot がコメントしたら、作業ブランチへ取り込み、競合を解消して再テスト・push する。手順は `docs/DEVELOPMENT.md` の「マージのキュー」。
- 不具合には回帰テストを追加する。見た目の変更は HIG・ゲーム UX 基準も満たす。未確認の記録を合格として生成しない。

## 本番公開を指示されたとき

`docs/DEPLOYMENT.md` の手順を実行する。公開の前に `node scripts/live-state.mjs` で「本番公開の実行（待機中・実行中）」を確かめ、実行中があれば新しく依頼せずその実行を追う（同じ SHA の二重公開・版番号の重なりを防ぐ）。公開指示がある場合は、版を省いて `scripts/request-release.ps1` または GitHub Actions の `Release GitHub Pages` を実行する（版は workflow が最新のタグの patch + 1 に決め、公開済みの SHA なら何もしない。担当者が版を指定したときだけ付ける）。最後の `Confirm published release` の成功と、固定 URL の実動作まで確認する。PR のマージ自体は公開指示ではない。既存の版・タグ・Release を上書きしない。

## 確認用プレビュー

`docs/PREVIEW.md` を読む。同一リポジトリの PR は Draft でも Preview build の成功後に本番とは別のサイトへ自動公開される。追加の公開指示は不要。PR の Bot コメントにある現在の SHA の URL を開き、表示と変更した操作を確認する。本番の Release ワークフローを代用しない。

早く確認できるまとまりで push し、Preview build の結果と URL を先に案内する。全画面テストや Copilot の終了をプレビューの待ち条件にしない。全検証・レビュー未完了の場合はその状態を明記し、完了扱いにしない。

## 作業時間を短くする

CI の分割数・長時間化の確認方法は `docs/CI_SCALING.md`。新しい E2E は `e2e/` に追加し、全体 CI の自動検出対象にする。遅いときは結果の時間計測を見て原因を直し、品質ゲートや端末構成を削って対処しない。

- 不明点が実装を左右するときだけ質問し、決定済みのマージ・プレビュー許可を繰り返し求めない。
- 一度読んだ仕様は差分と今回の対象を優先して読む。関係のない全体調査やリファクタリングを始めない。
- 変更に関係するテストをローカルで先に実行し、全構成の検証は CI と並行する。同じ SHA の成功結果を理由なく取り直さない。新しい変更・失敗・main 更新があれば必要な検証を行う。
- Copilot の観点・再依頼条件は `docs/COPILOT_REVIEW.md`。同一SHAの再依頼や通常の指摘修正だけの再レビューを行わない。Copilot は PR 作成時に1回自動依頼する補助レビュー。未契約・上限・障害なら待たず、作業者のレビューと必須 CI で進める。指摘が届いた場合は再現を確認して対応し、内容を読まず会話を解決済みにしない。

## 最後のリンクと QR コード

デプロイまたはプレビュー URL を作った場合は、最後の報告にクリックできるリンクと同じ URL の QR コード画像を必ず一緒に提示する。プレビューは固定 URL の `qr.png` を画像として表示する。QR の中身は同じ固定 URL であることを確認し、リンク先の応答・対象 SHA・画面を確認してから案内する。

本番など既存の QR がない URL は `pnpm qr <確認済みHTTPS-URL> <出力先.png>` でローカル生成する。QR は外部サイトへ転送せず生成し、読み戻しテストで URL 一致を検証している。画像添付不可の環境では QR 画像へのリンクを併記し、未生成の画像を提示しない。

## 素材を探すとき

画像集を使う作業では最初に [docs/ASSET_LIBRARY.md](docs/ASSET_LIBRARY.md) を読み、指定 Figma の ID と原画像・出典を参照する。世界観イラストと実物グッズ写真を区別する。内部検討用の画像を公開リポジトリ・公開プレビューへコピーしない。画面デザインの管理先は既存の DESIGN.md に従う。
